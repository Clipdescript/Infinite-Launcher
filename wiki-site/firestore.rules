rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles pour la collection articles
    match /articles/{articleId} {
      // Tout le monde peut lire et écrire (pas de sécurité)
      allow read, write: if true;
    }
  }
}
